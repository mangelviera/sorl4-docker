# Docker Image for Solr4

It includes plenty of dependencies to make it easier to start working from scratch with cores that uses a lot of plugins.

It's focused mostly to use on testing environments.
